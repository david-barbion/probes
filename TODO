everything:
- check db error, at least for writes and query run on user input
- add messages where needed
- add breadcrumbs ? or page tree path ?

draw show
- add interaction:
  * interval selection
  * show/hide series ?
- catch errors in graph query execution and display a message

draw data
- catch errors in graph query execution and display a message

draw edit

draw add

draw remove
